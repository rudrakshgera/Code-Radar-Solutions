#include <stdio.h>

int main() {
    int x;
    scanf("%d", &x);
    if (x<=0) {
        printf("Freezing", x,y);
    }
    else {
        printf("Above Freezing", x,y);
    }
  return 0;
}