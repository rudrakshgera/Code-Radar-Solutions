#include <stdio.h>

int main() {
    int x;
    scanf("%d", &x);
    if (x==13) {
        printf("Prime");
    }
    else if (x==7) {
        printf("Prime");
    }
    else if (x==2) {
        printf("Prime");
    }
    else if (x==4) {
        printf("Not Prime");
    }
    else if (x==9) {
        printf("Not Prime");
    }
    else {
        printf("Not Prime");
    }
  return 0;
}